#create in excel file to insert the data and save File → Save As → CSV (Comma delimited) (*.csv) select karo. in sem folder python file is here
import pandas as pd

df = pd.read_csv("Book1.csv")

print(df)
